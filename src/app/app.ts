import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [], // Add other components here if needed
  templateUrl: './app.html', // Make sure this matches your filename!
  styleUrl: './app.css'      // Make sure this matches your filename!
})
export class App {
  // Your logic here
}
